route:
  group_by: ['alertname', 'instance']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: 'telegram'

receivers:
- name: 'telegram'
  telegram_configs:
  - bot_token: '${BOT_TOKEN}'
    chat_id: ${CHAT_ID}
    parse_mode: 'HTML'
    api_url: 'https://api.telegram.org'

